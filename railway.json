{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "corepack enable && pnpm install --frozen-lockfile && pnpm --filter @trypact/sdk build && pnpm --filter @pact/indexer build && pnpm --filter @pact/mcp-http build"
  },
  "deploy": {
    "startCommand": "sh -c 'pnpm --filter @pact/${PACT_SERVICE:-indexer} start'",
    "healthcheckPath": "/healthz",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
