# X post drafts — PACT submission

Per PRD §18.1. 280-char limit. Hashtags + handles per HackQuest spec:
**`#0GHackathon #BuildOn0G @0G_labs @0g_CN @0g_Eco @HackQuest_`**

Three variants. Pick one, schedule for submission day morning UTC+8.

---

## Variant A — Technical (for crypto / Solidity Twitter)

> Built PACT in 14 days: a settlement protocol for AI-as-a-Service on
> 0G mainnet.
>
> Every settled job = a TEE-attested ECDSA signature recovered on-chain.
> Same primitive as personal_sign. 5 of 5 0G primitives load-bearing.
> First settled job → chainscan.0g.ai/tx/0xbb36752d…
>
> github.com/winsznx/pact
>
> #0GHackathon #BuildOn0G @0G_labs @0g_CN @0g_Eco @HackQuest_

**char count**: 272

---

## Variant B — Product (for builders / founders)

> AI agents need payment + trust rails on chain.
>
> PACT is the settlement layer for verifiable inference on 0G mainnet.
> Buyers pay per call. TEE attestation auto-releases the escrow.
> Reputation accrues to the seller's ERC-7857 INFT — sell the agent,
> sell the reputation.
>
> trypact.xyz
>
> #0GHackathon #BuildOn0G @0G_labs @0g_CN @0g_Eco @HackQuest_

**char count**: 277

---

## Variant C — Narrative (for general audience)

> "AI agents are about to become the largest economic actors in Web3.
> Today, no one can prove what model they ran."
>
> PACT solves that. Cryptographic settlement for AI inference on 0G.
> 7 mainnet contracts. 60 seconds end to end. Try it.
>
> trypact.xyz · github.com/winsznx/pact
>
> #0GHackathon #BuildOn0G @0G_labs @0g_CN @0g_Eco @HackQuest_

**char count**: 279

---

## Variant D — SDK launch (for dev twitter / npm-grep audience)

> 25 lines of TypeScript to pay an AI agent on 0G mainnet.
> Watch it through settlement. Cryptographically verify it ran the
> exact model + TEE the seller committed to.
>
> @trypact/sdk shipped today — `pnpm add @trypact/sdk viem`.
>
> trypact.xyz · npmjs.com/package/@trypact/sdk
>
> #0GHackathon #BuildOn0G @0G_labs @0g_CN @0g_Eco @HackQuest_

**char count**: 279

---

## Variant E — MCP launch (for the agentic-economy / Claude/Cursor audience)

> Your Claude / Cursor / Cline agent can now pay other AI agents on
> 0G mainnet, autonomously, with cryptographic proof. 6 lines in
> ~/.claude/mcp.json. No human approval per call.
>
> @trypact/mcp-server is live on npm. That's the agentic economy.
>
> npmjs.com/package/@trypact/mcp-server · trypact.xyz
>
> #0GHackathon #BuildOn0G @0G_labs @HackQuest_

**char count**: 268

---

## Optional thread tail (any variant)

If you go thread-style, tweet 2 + 3:

**Tweet 2 — The moat**:
> The moat: every inference returns a 5-field canonical text signed by
> the provider's TEE-bound key. AttestationVerifier.sol recovers the
> signer on-chain. If it doesn't match the registered key, the seller's
> bond slashes — 70% to disputer, 20% protocol, remainder burned.

**Tweet 3 — Verify yourself**:
> Verify yourself in your browser: trypact.xyz/verify/2
>
> Real captured TEE signature. Same EIP-191 + ECDSA primitive PACT runs
> on-chain. If it matches → attestation is authentic. If it doesn't →
> slash.

---

## Notes for Tim

- Add a 4-second demo GIF on Variant C — the ECDSARecoveryViz animation reaching the ✓ MATCH badge is the most viral frame.
- Schedule for May 16 ~9am UTC+8 (peak APAC engagement on submission day).
- After posting, reply to your own tweet with the chainscan link as proof-of-on-chain — judges check this.
- Final pre-tweet check: hover-preview the trypact.xyz link in the browser before publishing so the OG image renders correctly. The OG card is generated by [`apps/web/src/app/opengraph-image.tsx`](../apps/web/src/app/opengraph-image.tsx) (1200×630 PNG, deep-cosmos gradient, upright Instrument Serif headline, live mainnet stat strip) and inherits the same self-hosted fonts the production site ships.
